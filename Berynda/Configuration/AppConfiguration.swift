import Foundation

enum AppConfiguration {
    static let apiBaseURL = URL(string: "https://berynda.org/api/v1/")!
    static let supportURL = URL(string: "https://berynda.org/")!
    static let privacyURL = URL(string: "https://berynda.org/privacy/")!
}
